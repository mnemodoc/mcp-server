# frozen_string_literal: true

# because we heavily rely on it
brew 'bash'

# to install other tools
brew 'mise'

# for generating embeddings (native = Metal GPU on Apple Silicon)
brew 'ollama'

# envsubst, used by dev:vec0-objects to generate sqlite-vec.h
brew 'gettext'

# GNU timeout, which bounds dev:ameba; macOS ships no timeout at all
brew 'coreutils'
